1. Make sure that all dependencies are installed by running 
pip install -r requirements.txt
in the terminal

2. create a .env file in the root directory and add the following variables
OPENAI_API_KEY=YOUR API KEY HERE